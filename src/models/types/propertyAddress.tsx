export type PropertyAddress = {
    address: string,
    street: string,
    borough: string,
    unitNumber: string,
    postalCode: string,
    houseNumber: string,
    city?: string,
    schoolZones: string[],
}